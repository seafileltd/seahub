import React from 'react';
import ReactDOM from 'react-dom';
import { gettext as _ } from '../globals';

export class FileHistory extends React.Component {

  render() {
    return (
      <div>
        <h2>{_('Test React trans')}</h2>
      </div>
    )
  }
}
